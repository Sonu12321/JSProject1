let isarray = ['hello','he','she']
let arr = 0
while(arr<isarray.length){

 //   console.log(`my array is ${isarray[arr]}`);
    arr=arr+1
}
//forof loop
const s = [1,2,3,35]
for (const num of s ) {
    let num = 0
    if (num<arr.length){
        console.log(num);
}
}